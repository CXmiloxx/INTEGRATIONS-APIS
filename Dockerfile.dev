FROM node:20-slim

WORKDIR /app

RUN npm install -g pnpm

COPY package.json pnpm-lock.yaml ./

RUN pnpm install --frozen-lockfile

# instalar navegadores + deps del sistema
RUN pnpm exec playwright install --with-deps

COPY . .

EXPOSE 3145

CMD ["pnpm", "start:dev"]
